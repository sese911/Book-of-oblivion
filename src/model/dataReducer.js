// Action types

// Action creators

// Thunk creators

// Initial data
import initialData from "./initialData.json";
// Reducer
function dataReducer(state = {}, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default dataReducer;